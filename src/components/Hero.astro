---
const currentYear = new Date().getFullYear();
import { Image } from 'astro:assets';
import { Picture } from 'astro:assets';
import hero from '../assets/jdr-cover-01a.jpg';
---

<header class="sticky top-0 z-10 flex flex-col h-screen md:h-svh md:grid md:grid-cols-2">
	<div
		class="relative flex items-center justify-center h-full bg-center bg-cover visual md:h-screen"
	>
		<Image
			src={hero}
			alt="Jeffrey de Roode"
			widths={[720, 960, 1920]}
			sizes="(max-width: 360px) 720px, (max-width: 720px) 960px, (max-width: 1600px) 1920px, 100vw"
			class="top-0 h-full w-full object-cover"
		/>
	</div>

	<div class="flex items-center justify-center z-50 py-16 md:p-4 copy">
		<div class="text-center flex flex-col gap-2 p-vw-2
		 md:gap-4">
			<p class="uppercase intro text-7xl p-2 md:text-5xl lg:text-5xl text-balance leading-tighter">
				I am a Rotterdam based producer of documentaries and I work on projects that contribute to
				making this world a better place.
			</p>
			<p class="text-xs note">Feel free to reach out.</p>
		</div>
	</div>
</header>

<style>
	header {
		background: var(--text);
		/* background: red; */
		color: var(--clr-black);
		gap: var(--gapborder);
		/* border-bottom: var(--gapborder) solid var(--text); */
		min-height: -webkit-fill-available;
	}

	.visual {
		background: var(--bg);
	}
	.copy {
		background: var(--clr-brand);
	}

	.intro {
		font-family: var(--font-brand);
		color: var(--clr-black);
		font-weight: 100;
	}

	.intro .note {
		font-family: var(--font-body);
		font-weight: 600;
	}

	@media (min-width: 768px) {
		header {
			min-height: 100vh;
			grid-template-rows: 1fr;
			grid-template-columns: 1fr 1fr;
		}
	}
</style>
