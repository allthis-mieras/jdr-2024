---
const currentYear = new Date().getFullYear();
---

<header class="gap-0 bg-white md:grid l:grid-cols-2">
	<div
		class="visual parallax flex relative items-center justify-center sm:relative md:h-screen md:sticky md:top-0"
	>
		<img
			class="absolute top-0 z-10 object-cover w-full h-full"
			src="./assets/images/jdr-cover.jpg"
			alt="Jeffrey de Roode"
		/>
	</div>

	<div class="intro p-vw-4 pt-vw-32 pb-vw-12 text-center tw">
		<p class="text-7xl uppercase">
			I work on films that broaden horizons and share new perspectives.
		</p>
	</div>
</header>

<style>
	header {
		position: relative;
		background: var(--clr-brand);
		color: var(--clr-black);
		z-index: 10;
	}

	.visual {
		background: var(--bg);
		z-index: 10;
		min-height: 50vh;
	}

	.intro {
		font-family: var(--font-brand);
		color: var(--clr-black);
		font-weight: 100;
	}

	.centered {
		position: absolute;
		top: 50%;
		left: 50%;
		transform: translate(-50%, -50%);
		text-align: center;
		height: 60%;
	}

	@media (min-width: 768px) {
		header {
			min-height: 100vh;
			grid-template-rows: 1fr;
			grid-template-columns: 1fr 1fr;
		}
	}
</style>
